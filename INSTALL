REQUIREMENTS

libext library:
	git clone https://github.com/ufasoft/libext --recursive
	cd libext
	autoreconf && ./configure && make && sudo make install



HOW TO BUILD

	autoreconf && ./configure && make && sudo make install

modify systemd unit (sysvinit script) to call the daemon with listening IP/port options:
	/usr/local/bin/socksd -l 192.168.0.1 -p 1080






